# ChatEarn

Single production source for the ChatEarn website deployed from `main`.

Production entry point: `index.html`

The repository intentionally contains only the files required by the current production website.
