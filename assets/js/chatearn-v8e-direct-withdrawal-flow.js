/* ChatEarn V8E.2 — direct, persistent and lightweight withdrawal journey. */
(() => {
  'use strict';
  if (window.__CHAT_EARN_V8E2_DIRECT_WITHDRAWAL__) return;
  window.__CHAT_EARN_V8E2_DIRECT_WITHDRAWAL__=true;
  const VERSION='8E.2',DRAFT_KEY='ce_withdrawal_draft_v8e',FLOW_KEY='ce_withdrawal_flow_v8e',IDEMPOTENCY_KEY='ce_direct_withdrawal_key';
  const byId=id=>document.getElementById(id),toast=m=>window.showToast?.(m);
  const getClient=()=>{try{if(typeof supabaseClient!=='undefined'&&supabaseClient?.rpc)return supabaseClient}catch(_){}if(window.supabaseClient?.rpc)return window.supabaseClient;throw new Error('Secure connection is still loading. Please try again.')};
  const rpc=async(name,args={})=>{const{data,error}=await getClient().rpc(name,args);if(error)throw error;return typeof data==='string'?JSON.parse(data):data};
  const loadDraft=()=>{try{return JSON.parse(localStorage.getItem(DRAFT_KEY)||'{}')}catch(_){return{}}},saveDraft=d=>localStorage.setItem(DRAFT_KEY,JSON.stringify(d));
  const loadFlow=()=>{try{return JSON.parse(localStorage.getItem(FLOW_KEY)||'null')}catch(_){return null}},saveFlow=f=>localStorage.setItem(FLOW_KEY,JSON.stringify({...f,updated_at:Date.now()}));
  const BANKS=[['opay','OPay'],['palmpay','PalmPay'],['kuda','Kuda Bank'],['moniepoint','Moniepoint'],['gtbank','GTBank'],['access','Access Bank'],['firstbank','First Bank'],['uba','UBA'],['zenith','Zenith Bank'],['fcmb','FCMB'],['fidelity','Fidelity Bank'],['sterling','Sterling Bank'],['wema','Wema Bank / ALAT'],['union','Union Bank'],['stanbic','Stanbic IBTC'],['ecobank','Ecobank'],['polaris','Polaris Bank'],['keystone','Keystone Bank'],['providus','Providus Bank'],['other','Other Nigerian bank']];
  function secureBackNotice(){const m=byId('backWarn');if(!m)return;const t=m.querySelector('.bw-title'),b=m.querySelector('.bw-body'),s=m.querySelector('.btn-bw-stay'),l=m.querySelector('.btn-bw-leave');if(t)t.textContent='Your progress is saved';if(b)b.textContent='Your earnings and withdrawal progress are securely stored.';if(s)s.textContent='Continue current step';if(l){l.textContent='Return to dashboard';l.onclick=()=>{window.closeBackWarn?.();window.goScreen?.('dashboard')}}}
  function cleanOldUi(){byId('cePayoutAccountForm')?.remove();const g=document.querySelector('#withdraw .bank-options')?.closest('.form-group');if(g)g.style.display='none';document.querySelector('#withdraw .btn-place-wd')?.style.setProperty('display','none','important')}
  function ensureDirectForm(){const body=document.querySelector('#withdraw .wd-body');if(!body)return;cleanOldUi();if(byId('ceDirectWithdrawalForm'))return;const d=loadDraft(),f=document.createElement('form');f.id='ceDirectWithdrawalForm';f.style.cssText='margin:0 0 18px;padding:17px;border:1px solid var(--line);border-radius:17px;background:var(--card2);display:grid;gap:12px';f.innerHTML=`<div style="font-size:16px;font-weight:900">Where should we send your withdrawal?</div><div style="font-size:12px;color:var(--muted)">Your progress is saved automatically.</div><label>Bank</label><select id="ceDirectBank" required style="padding:14px;border-radius:12px;background:var(--card);color:var(--white)">${BANKS.map(([v,l])=>`<option value="${v}">${l}</option>`).join('')}</select><label>Account name</label><input id="ceDirectAccountName" required maxlength="120" placeholder="Name on the bank account" style="padding:14px;border-radius:12px;background:var(--card);color:var(--white)"><label>Account number</label><input id="ceDirectAccountNumber" required inputmode="numeric" maxlength="10" placeholder="10-digit account number" style="padding:14px;border-radius:12px;background:var(--card);color:var(--white)"><button type="submit" style="padding:15px;border:0;border-radius:13px;background:#00c853;font-weight:900">Place my withdrawal now →</button><div style="font-size:11px;color:var(--muted)">Next: compulsory sharing, then KYC and payment processing.</div>`;body.querySelector('.wd-amount-display')?.insertAdjacentElement('afterend',f);if(!f.isConnected)body.prepend(f);const bank=byId('ceDirectBank'),name=byId('ceDirectAccountName'),number=byId('ceDirectAccountNumber');bank.value=d.bank||'opay';name.value=d.account_name||'';number.value=d.account_number||'';const persist=()=>saveDraft({bank:bank.value,account_name:name.value.trim(),account_number:number.value.replace(/\D/g,'').slice(0,10)});bank.onchange=persist;name.oninput=persist;number.oninput=()=>{number.value=number.value.replace(/\D/g,'').slice(0,10);persist()};f.onsubmit=async e=>{e.preventDefault();const btn=f.querySelector('button'),accountName=name.value.trim(),accountNumber=number.value.replace(/\D/g,'').slice(0,10);if(accountName.length<3)return toast('Enter the account name.');if(accountNumber.length!==10)return toast('Enter a valid 10-digit account number.');persist();btn.disabled=true;btn.textContent='Securing withdrawal…';let key=sessionStorage.getItem(IDEMPOTENCY_KEY);if(!key){key=`direct-${crypto.randomUUID()}`;sessionStorage.setItem(IDEMPOTENCY_KEY,key)}try{const stateAmount=Number(window.ChatEarnWithdrawalV5?.getState?.()?.portal?.wallet?.available_balance||0);let fallback=0;try{fallback=Number(totalBalance||0)}catch(_){}const r=await rpc('chatearn_place_withdrawal_direct_v6',{p_provider:bank.value,p_account_number:accountNumber,p_account_name:accountName,p_amount:stateAmount||fallback,p_idempotency_key:key});if(r?.ok===false)throw new Error(r.message||'Withdrawal could not be placed.');saveFlow({withdrawal_id:r.withdrawal_id,stage:'sharing_required'});toast('Withdrawal placed. Complete the required sharing step.');window.goScreen?.('sharewall')}catch(err){toast(err.message||'Withdrawal could not be placed.')}finally{btn.disabled=false;btn.textContent='Place my withdrawal now →'}}}
  function showContinue(){const p=byId('processing');if(!p||byId('ceContinueEarningBtn'))return;const w=document.createElement('div');w.style.cssText='margin-top:18px;text-align:center;max-width:430px;padding:0 20px';w.innerHTML='<div style="margin-bottom:12px">Your payment is processing securely. You can continue earning.</div><button id="ceContinueEarningBtn" style="width:100%;padding:14px;border:0;border-radius:13px;background:#00c853;font-weight:900">Continue earning →</button>';p.appendChild(w);byId('ceContinueEarningBtn').onclick=async()=>{const flow=loadFlow(),btn=byId('ceContinueEarningBtn');btn.disabled=true;try{if(flow?.withdrawal_id)await rpc('chatearn_resume_earning_after_withdrawal_v6',{p_withdrawal_id:flow.withdrawal_id});localStorage.setItem('ce_returning_user','1');saveFlow({withdrawal_id:flow?.withdrawal_id||null,stage:'processing'});window.ChatEarnV8DFlow?.openNextConversation?.()||window.goScreen?.('dashboard')}catch(e){toast(e.message||'Next earning session could not open.');btn.disabled=false}}}
  function resume(){const f=loadFlow();if(!f?.stage)return;let s='';try{s=currentScreen||''}catch(_){}if(f.stage==='sharing_required'&&['withdraw','processing'].includes(s))window.goScreen?.('sharewall');else if(f.stage==='kyc_required'&&s==='withdraw')window.goScreen?.('kyc')}
  const original=window.goScreen;if(typeof original==='function')window.goScreen=function(id){const r=original(id);if(id==='withdraw')setTimeout(ensureDirectForm,40);if(id==='processing')setTimeout(showContinue,40);return r};
  function boot(){secureBackNotice();let s='';try{s=currentScreen||''}catch(_){}if(s==='withdraw')ensureDirectForm();if(s==='processing')showContinue();resume()}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();window.addEventListener('pageshow',()=>setTimeout(boot,100));
  window.ChatEarnV8EDirectWithdrawal=Object.freeze({version:VERSION,ensureDirectForm,resumeSavedFlow:resume,diagnostic:()=>({version:VERSION,flow:loadFlow()})});
  console.info(`[ChatEarn] Lightweight withdrawal flow ${VERSION} loaded`);
})();